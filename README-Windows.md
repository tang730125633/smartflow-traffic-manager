# Windows 本地运行指南

## ✅ 前置条件检查

运行 `check-services.bat` 检查所有必备软件是否安装成功。

## 🚀 启动顺序

**重要：必须按顺序启动，每个服务启动后等待一会儿再启动下一个！**

### 1. 启动 Eureka Server（服务注册中心）

双击运行：`start-eureka.bat`

等待看到：
```
Started EurekaServerApplication in X seconds
```

然后打开浏览器访问：http://localhost:8761
应该能看到 Eureka 控制台。

### 2. 启动 Gateway Service（API网关）

双击运行：`start-gateway.bat`

等待看到：
```
Started GatewayServiceApplication in X seconds
```

### 3. 启动 Auth Service（认证服务）【可选】

双击运行：`start-auth.bat`

等待看到：
```
Started AuthServiceApplication in X seconds
```

### 4. 启动 Flask 前端【可选】

双击运行：`start-frontend.bat`

访问：http://localhost:8080 或 http://localhost:5000

## 📊 访问地址

- **Eureka 控制台**: http://localhost:8761
- **API 网关**: http://localhost:8080
- **前端界面**: http://localhost:8080（或 5000）

## 🛑 停止服务

直接关闭对应的 cmd 窗口，或按 `Ctrl+C`

## ⚠️ 常见问题

### 1. 端口被占用

```cmd
# 查看占用进程
netstat -ano | findstr :8080

# 杀死进程
taskkill /PID <进程ID> /F
```

### 2. Maven 下载依赖很慢

确认已配置阿里云镜像（见安装步骤）

### 3. 服务无法注册到 Eureka

- 确保 Eureka Server 已经完全启动
- 等待 30 秒后刷新 http://localhost:8761

### 4. MySQL 连接失败

检查配置：
- 用户名：root
- 密码：root
- 地址：localhost:3306

## 💡 最简启动方案

如果只想快速测试，只需要启动：

1. **start-eureka.bat** - Eureka 服务注册中心
2. **start-frontend.bat** - Flask 前端

这样就能看到基本界面了。

## 🔧 编译优化（可选）

如果想加快启动速度，可以先编译所有服务：

```cmd
cd spring-cloud-services
mvn clean package -DskipTests
```

编译完成后，可以直接运行 jar 包：

```cmd
java -jar eureka-server\target\eureka-server-0.0.1-SNAPSHOT.jar
```

## 📝 注意事项

- 第一次启动会下载很多依赖，需要等待较长时间（10-30分钟）
- 确保网络稳定，Maven 已配置国内镜像
- 如果内存不足（<8GB），建议只启动 2-3 个核心服务
- Kafka 相关功能暂时无法使用（需要额外安装）
